/**
 * This file is empty as you should generate it using sequelize-auto.
 * When running the command, make sure you set the language to ts and
 * after copying the files from models, remove any "property!" with
 * "declare property" as you shadow the property in the model.
 *
 * @link https://github.com/sequelize/sequelize-auto
 */

export function initModels(prop: unknown) {
  return prop;
}
